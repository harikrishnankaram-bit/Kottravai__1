const HeroSlider = () => {
    return (
        <section className="relative w-full">
            <img
                src="/hero.webp"
                alt="Kottravai Hero"
                className="w-full h-auto block"
                loading="eager"
            />
        </section>
    );
};

export default HeroSlider;
